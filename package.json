{
  "name": "design-tokens",
  "version": "1.0.0",
  "description": "Shared design tokens for harini (Next.js) and my-b2c-app (React Native)",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "build:watch": "nodemon --watch tokens --exec 'npm run build'"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.1"
  },
  "keywords": ["design-tokens", "figma", "style-dictionary"],
  "private": true
}
